{
    "name": "Chatbot",    
    "env": {
        "BOT_TOKEN": {
            "description": "A Bot Token from @Botfather",
            "required": true
        }              
    },
    "addons": [
        {
            "plan": "heroku-postgresql"
        }
    ]
}
